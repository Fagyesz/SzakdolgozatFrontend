import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.appoint.alpha',
  appName: 'src',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
